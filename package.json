{
  "name": "agent-dogfeed",
  "version": "0.2.0",
  "private": true,
  "description": "SUPERSEDED by agent-env — compatibility shim that delegates the old probe/capture surface to the agent-env CLI.",
  "license": "MIT",
  "type": "module",
  "bin": {
    "agent-dogfeed": "bin/agent-dogfeed.mjs"
  },
  "scripts": {
    "test": "node bin/agent-dogfeed.mjs claude --repo /tmp --prompt smoke > /dev/null && echo shim-ok"
  }
}
